# --- Database ---
# Postgres connection string (no pgvector needed for this project — it's not RAG-based).
DATABASE_URL="postgresql://user:password@localhost:5432/aviation_ground_school"

# --- Auth ---
# Random 32+ byte secret used to sign session JWTs.
JWT_SECRET="change-me-to-a-long-random-string"

# --- Anthropic Claude (ground-school chatbot brain) ---
ANTHROPIC_API_KEY="sk-ant-..."
ANTHROPIC_MODEL="claude-sonnet-5"

# --- Stripe (student subscriptions / paywall) ---
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
# Price IDs for the paid plan(s) students subscribe to once their free trial runs out.
STRIPE_PRICE_MONTHLY="price_..."
STRIPE_PRICE_ANNUAL="price_..."

# --- Stripe Connect (instructor payouts + platform commission) ---
# Same Stripe account as above; Connect uses STRIPE_SECRET_KEY. This is the webhook
# secret for the *Connect* webhook endpoint (separate endpoint/secret from the one above).
STRIPE_CONNECT_WEBHOOK_SECRET="whsec_..."
# Platform commission taken out of every paid (non-trial) instructor session, in basis points.
# 1500 = 15%.
PLATFORM_COMMISSION_BPS=1500

# --- App ---
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="Ground School AI"

# Free-trial config for the chatbot (messages a student gets before the paywall kicks in).
FREE_CHAT_MESSAGE_LIMIT=20

# Length of a student's free first session with any given instructor, in minutes.
FREE_INSTRUCTOR_SESSION_MINUTES=30

# --- Session booking ---
# Students book any duration on this step, between these bounds (e.g. 30 -> 30/60/90/120/150/180 min).
MIN_SESSION_MINUTES=30
MAX_SESSION_MINUTES=180
SESSION_DURATION_STEP_MINUTES=30
# How far in advance a session must be booked.
MIN_BOOKING_NOTICE_MINUTES=60

# --- Daily.co (video calls for booked sessions) ---
# Optional: booking + payment work without it, but confirmed sessions won't get a video room
# (and /session/:bookingId will show "video isn't set up") until this is set.
# Get one at https://dashboard.daily.co/developers
DAILY_API_KEY="..."

# --- Email (SMTP) ---
# Optional: without this, every email send logs a warning and no-ops instead of failing —
# signup, password reset, and booking emails all still "work" in the sense that nothing
# breaks, they just don't actually deliver anything.
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="Ground School AI <no-reply@groundschool.ai>"

# --- Cron (reminder emails + booking-completion sweep) ---
# Shared secret required on Authorization: Bearer <CRON_SECRET> for GET /api/cron/reminders.
# On Vercel, set this env var and Vercel Cron sends it automatically — see vercel.json.
CRON_SECRET="change-me-to-a-long-random-string"
# How far ahead of a session's start time to send the "starting soon" reminder email.
REMINDER_LEAD_MINUTES=15
