task 1 check the whole app for problems

task 2 (SEO) — Improve SEO across the public marketing pages (index.html, pricing.html, signup.html, contact.html, characters.html, public/docs/*.html). Add a unique <meta name="description"> to every page that's missing one (only index.html has one today), plus Open Graph + Twitter Card tags (og:title, og:description, og:image, og:url, twitter:card) and a canonical <link> tag. Add a favicon <link rel="icon"> (none exists anywhere right now). Add public/robots.txt that allows the marketing + docs pages but disallows /dashboard, /project, /billing, /analytics, /account — these are served by backend/server.js as static HTML with no server-side auth gate on the page shell itself, so they're crawlable and shouldn't be indexed. Add public/sitemap.xml listing the public marketing + docs URLs and reference it from robots.txt. Add JSON-LD structured data (SoftwareApplication/Product schema) to index.html. Double check heading hierarchy (one <h1> per page, no skipped levels) on every page.

task 3 (Performance) — helmet() and compression() are already enabled in backend/server.js, but express.static(PUBLIC_DIR) (backend/server.js:112) has no Cache-Control/maxAge config, so every CSS/JS asset gets re-fetched on every page load. Add long-lived immutable caching for static assets there. Audit public/lipsync-sdk.js (84KB, unminified) — this is the widget script every customer site embeds — minify/bundle it for production and confirm the <script> tag that loads it uses async/defer so it never blocks the host page's render. Confirm the new backend/cache.js is actually wired into the hot paths (/embed/:id/config and /embed/:id/retrieve in backend/routes/embed.js) rather than sitting unused. Check backend/services/embed.js and backend/services/vector.js for batching opportunities on Gemini embedding calls and pgvector search. Review query patterns in backend/routes/analytics.js and projects.js (sessions/messages/leads) for missing indexes beyond the existing chunks HNSW index, and sanity-check the pg.Pool sizing in backend/db.js against expected concurrent embed traffic.

task 4 (Fix issues) — Full correctness pass: re-verify auth (signup/login/reset-password in backend/routes/auth.js), Stripe billing + webhook idempotency (backend/routes/billing.js, backend/services/stripe.js), and the file ingestion pipeline (backend/services/process.js → chunk.js → embed.js) surfaces errors correctly through the /status polling endpoint. Confirm the two previously-fixed bugs (dead embedding model, missing user transcription in the Gemini Live flow) haven't regressed. Several new files aren't confirmed wired up yet — check each is actually imported and used, or remove if dead: backend/cache.js, backend/logger.js, backend/middleware/validate.js, backend/services/email.js, public/js/amplitude-fallback.js, public/js/audio-clock.js, public/js/hybrid-lipsync-controller.js, public/js/viseme-map.js, public/sdk/. In particular, if backend/middleware/validate.js isn't applied to routes yet, that's a missing input-validation gap worth flagging.
